# ============================================
# CoExistree Nginx 配置
#
# 默认：HTTP only (端口 80)
# 如需 HTTPS：取消下方 HTTPS server 段的注释，并配置 SSL 证书
# ============================================

upstream backend {
    server backend:8080;
}

# ============================================
# HTTP Server (端口 80)
# ============================================
server {
    listen 80;
    server_name _;

    # 安全响应头
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    # 文件上传大小限制
    client_max_body_size 50M;

    # 健康检查
    location /actuator/health {
        proxy_pass http://backend/actuator/health;
        access_log off;
    }

    # Actuator 端点（限制本地和 Docker 网络访问）
    location /actuator/ {
        allow 127.0.0.1;
        allow 172.16.0.0/12;
        deny all;

        proxy_pass http://backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        access_log off;
    }

    # API 代理到后端
    location /api/ {
        proxy_pass http://backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # 静态文件
    location / {
        root /usr/share/nginx/html;
        try_files $uri $uri/ /index.html;
    }

    # 文档下载
    location /docs/ {
        alias /data/docs/;
        autoindex off;
        expires 7d;
    }
}


# ============================================
# HTTPS Server (端口 443)
# ============================================
# 如需启用 HTTPS：
#   1. 准备 SSL 证书文件放到 nginx/ssl/ 目录：
#      - fullchain.pem (证书+中间证书)
#      - privkey.pem (私钥)
#
#   2. 修改 docker-compose.yml，添加 443 端口映射：
#      ports:
#        - "80:80"
#        - "443:443"    # 取消这行注释
#      volumes:
#        - ./nginx/ssl:/etc/nginx/ssl:ro   # 取消这行注释
#
#   3. 取消下方整段的注释（从 # server { 到最后的 # }
# ============================================

# server {
#     listen 443 ssl http2;
#     server_name _;
#
#     # SSL 证书配置
#     ssl_certificate /etc/nginx/ssl/fullchain.pem;
#     ssl_certificate_key /etc/nginx/ssl/privkey.pem;
#
#     # SSL 安全配置
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
#     ssl_prefer_server_ciphers off;
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 1d;
#
#     # 安全响应头
#     add_header X-Frame-Options "SAMEORIGIN" always;
#     add_header X-Content-Type-Options "nosniff" always;
#     add_header X-XSS-Protection "1; mode=block" always;
#     add_header Referrer-Policy "strict-origin-when-cross-origin" always;
#     add_header Strict-Transport-Security "max-age=63072000" always;
#
#     # 文件上传大小限制
#     client_max_body_size 50M;
#
#     # 健康检查
#     location /actuator/health {
#         proxy_pass http://backend/actuator/health;
#         access_log off;
#     }
#
#     # Actuator 端点
#     location /actuator/ {
#         allow 127.0.0.1;
#         allow 172.16.0.0/12;
#         deny all;
#
#         proxy_pass http://backend;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         access_log off;
#     }
#
#     # API 代理到后端
#     location /api/ {
#         proxy_pass http://backend;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_connect_timeout 60s;
#         proxy_send_timeout 60s;
#         proxy_read_timeout 60s;
#     }
#
#     # 静态文件
#     location / {
#         root /usr/share/nginx/html;
#         try_files $uri $uri/ /index.html;
#     }
#
#     # 文档下载
#     location /docs/ {
#         alias /data/docs/;
#         autoindex off;
#         expires 7d;
#     }
# }
#
#
# ============================================
# HTTP 重定向到 HTTPS（可选）
# ============================================
# 如果启用了 HTTPS，可以把上面的 HTTP server (端口 80) 改成只重定向：
#
# server {
#     listen 80;
#     server_name _;
#
#     # 只保留 ACME 验证（Let's Encrypt 续期用）
#     location /.well-known/acme-challenge/ {
#         root /var/www/certbot;
#     }
#
#     # 其他请求重定向到 HTTPS
#     location / {
#         return 301 https://$host$request_uri;
#     }
# }
# ============================================
